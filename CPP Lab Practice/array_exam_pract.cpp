#include <iostream>
#include <string>
using namespace std;

main()

{
	//table of 10
	
	/*int arr[5];
	int i;
	for(int i = 1; i<=5; i++)
	{
		cout<<"Enter the elements of array = ";
		cin>>arr[i];
	}
	
		cout << "Enter the element's index number you want to display";
		cin>>i;
		cout<< "your desired element at index [" <<i<<"] is = "<<arr[i];
	
	return 0;*/
	
	
	// 10 tables
	
	/*int n,i;
	 
	 //cout << "Enter the number whose table you want to display = ";
	 //cin >> n;
	 for (int j = 1; j<=10; j++)
	 {
	 
	 for(int i = 1; i <= 10; i++)
	 {
	 
	 cout << j << "*" << i << "=" << j*i << endl;
	 
}

	 cout << "\n \n ************************************************** \n\n";
	 */
	 
	 
	 
	 // string character
	 
	 
	/* string one = "yes yes";
	 string two = " no no";
	 
	 one.assign(two);
	 
	 cout<<" string one is = \n\n"<< one;
	 cout<<" string 2 is = "<< two;*/
	 
	 
//	 swaping strings
	 /*string one = "yes yes";
	 string two = " no no";
	 
	 one.swap(two);
	 
	 cout<<" string one is = "<< one;
	 cout<<"\n\n string 2 is = "<< two; */
	 
	 
//	 array display
	 /*int x[10];
	 
	 for(int i = 1; i<=10; i++)
	 {
	 
	 cout<<"number of elements in an array = ";
	 cin>>x[i];
}

for(int i = 1; i<=10; i++)
{
	int k= i;
	 cout<<"\n\nyour array is sorted as ["<<i<<"] = "<< x[k];
} */



	 
	 
	 
	 
	 
}
