
#include<iostream> 
using namespace std; 
int sum(int x); 
int y;
int main() { 
 	  	 
 	cout<<"Enter a number: ";  	
	cin>>y; 
 	cout<<"Sum from 1 to "<<y<<" is "<<sum(y); 
 	 
 	return 0; 
} 
int sum(int x) 
{ 
	for(x = 0; x<y; x++)
 	x = x+(x+2);
 	return x;
 	 
} 
